server:
  port: 8081
spring:
  application:
    name: gateway
  cloud:
    config:
      uri: http://localhost:8888
---
spring:
  profiles: docker
  cloud:
    config:
      uri: http://barbershop-config:8888

